

## Hide the Newsletter Signup

Remove the `NewsletterSignup` component from the Index page. The component file stays in place for future use — just remove its rendering.

### File to edit
- `src/pages/Index.tsx` — Remove the `NewsletterSignup` import and its JSX usage

